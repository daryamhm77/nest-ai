# Gemini — Entry Point

Follow the shared contract in `CLAUDE.md`. Same boot sequence, same hard rules.

## Gemini-specific notes
- Use the large context window to load `00-project/*` and the full relevant
  `01-rules/**` subtree at once before answering.
- Keep code blocks self-contained and runnable.
- Default output mode: `10-output/concise.md`.
