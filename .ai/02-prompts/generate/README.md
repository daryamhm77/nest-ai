# Generate — Prompt Fragment
Use when creating new code.
> Generate <artifact> for <feature>. Follow `06-templates/<artifact>.md` and the
> rules in `01-rules/**`. Use glossary terms from `00-project/glossary.md`.
> Include tests. List the rule/template files you applied.
