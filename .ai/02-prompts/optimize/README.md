# Optimize — Prompt Fragment
> Optimize <target> for <latency/memory/throughput>. Measure first (state assumptions),
> propose the change, predict the impact, and add/adjust a benchmark or test.
> Do not sacrifice correctness or readability without justification.
