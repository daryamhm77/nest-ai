# Example: grpc

Proto contract, server + client, interceptors for auth/tracing, status mapping.

Place runnable reference code here. It must obey every file in `01-rules/**` and serve as the canonical pattern other features copy.
